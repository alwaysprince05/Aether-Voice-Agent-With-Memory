version: '3.8'

services:
  voice-agent-app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - agent_data:/root/.voice-agent

volumes:
  agent_data:
